{#
/**
 * @file
 * Component for a branding header.
 *
 * Each branding element variable (logo, name, slogan) is only available if
 * enabled in the block configuration.
 *
 * Available variables:
 * - site_logo: Logo for site as defined in Appearance or theme settings.
 * - site_name: Name for site as defined in Site information settings.
 * - site_slogan: Slogan for site as defined in Site information settings.
 * - root_path: Variable for drupal root path using path() function on drupal side and "/" in styleguide
 */
#}
{{ attach_library('dddlx/branding') }}
<div class="branding__wrapper layout-center">
  {% if site_logo %}
    <a href="{{ root_path }}" title="{{ 'Home'|t }}" rel="home" class="branding__logo">
      <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" class="branding__logo-image"/>
    </a>
  {% endif %}
  {# {% if site_name or site_slogan %}
    <div class="branding__name-and-slogan">
      {% if site_name %}
        <h1 class="branding__site-name">
          <a href="{{ root_path }}" title="{{ 'Home'|t }}" rel="home" class="branding__site-link"> {{ site_name }}</a>
        </h1>
      {% endif %}
      {% if site_slogan %}
        <div class="branding__site-slogan">{{ site_slogan }}</div>
      {% endif %}
    </div>
  {% endif %} #}

    <div class="branding__name-and-slogan visually-hidden">
      <h1 class="branding__site-name">
        <a href="{{ root_path }}" title="{{ 'Home'|t }}" rel="home" class="branding__site-link"> {{ site_name }}</a>
      </h1>
    </div>

</div>
